# ---

# - name: Sartting Reporting tasks
#   hosts: all
#   gather_facts: true
#   become: true
#   tasks:
#     - name: Generating Patching report
#       ansible.builtin.template:
#         src: report_before_patching.j2
#         dest: "/opt/patching_reports/patch_report_{{ inventory_hostname }}_{{ ansible_date_time.date }}.html"