plugin: amazon.aws.aws_ec2
profile: ansibleadmin
regions:
  - us-east-1
keyed_groups:
  - key: tags.Environment
compose:
  ansible_host: private_ip_address
filters:
  instance-state-name: running
leading_separator: false
use_contrib_script_compatible_sanitization: false